// config/tsup/constants.ts

export const DEFAULT_TARGET = 'node22';

export const DEFAULT_FORMAT = ['esm'] as const;

export const DEFAULT_OUT_DIR = 'dist';

export const DEFAULT_ENTRY = 'src/index.ts';
