{
  "name": "@arch-platform/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "files": [
    "dist",
    "bin"
  ],
  "arch": {
    "artifactType": "runtime",
    "build": {
      "builder": "tsup",
      "outputs": [
        "dist"
      ]
    },
    "kind": "app"
  },
  "bin": {
    "arch": "./bin/arch.js"
  },
  "scripts": {
    "arch": "arch build --package @arch-platform/cli",
    "build": "tsup",
    "clean": "arch clean",
    "compliance": "arch compliance --package @arch-platform/cli --env dev",
    "typecheck": "arch typecheck"
  },
  "dependencies": {
    "@arch-platform/governance": "workspace:*",
    "@arch-platform/tooling": "workspace:*",
    "@arch-platform/infrastructure": "workspace:*",
    "cac": "catalog:"
  }
}
