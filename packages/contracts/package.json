{
  "name": "@arch/contracts",
  "version": "0.1.0",
  "type": "module",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts"
    }
  },
  "arch": {
    "build": {
      "builder": "tsc-declaration",
      "outputs": [
        "dist"
      ]
    }
  },
  "scripts": {
    "build": "pnpm build:dts",
    "build:dts": "tsc -p tsconfig.build.json",
    "arch": "arch build --package @arch/contracts",
    "clean": "arch clean",
    "typecheck": "arch typecheck"
  }
}
