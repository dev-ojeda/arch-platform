{
  "name": "@arch-platform/application",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "arch": {
    "artifactType": "runtime",
    "build": {
      "builder": "tsc",
      "outputs": [
        "dist"
      ],
      "validateExports": true
    },
    "kind": "app"
  },
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "arch": "arch build --package @arch-platform/application",
    "clean": "arch clean",
    "compliance": "arch compliance --package @arch-platform/application --env dev",
    "typecheck": "arch typecheck",
    "test": "vitest run -c ./vitest.config.ts",
    "coverage": "vitest run --coverage -c ./vitest.config.ts",
    "test:watch": "vitest",
    "test:clear": "vitest --clearCache"
  },
  "dependencies": {
    "@arch-platform/contracts": "0.1.0",
    "@arch-platform/core": "workspace:*",
    "@arch-platform/generator-mvc": "0.1.0",
    "@arch-platform/testing": "workspace:*"
  },
  "devDependencies": {
    "handlebars": "^4.7.9"
  }
}
