{
  "name": "@arch/infrastructure",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "types": "./dist/index.d.ts",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "arch": {
    "builder": "tsc"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "node ../cli/dist/bin.js lint",
    "arch:build": "arch build --package @arch/infrastructure",
    "arch:clean": "arch clean",
    "arch:lint": "arch lint",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run -c ./vitest.config.ts",
    "coverage": "vitest run --coverage -c ./vitest.config.ts",
    "test:watch": "vitest",
    "test:clear": "vitest --clearCache"
  },
  "dependencies": {
    "@arch/contracts": "workspace:*",
    "@arch/core": "workspace:*",
    "@arch/testing": "workspace:*"
  },
  "devDependencies": {
    "handlebars": "^4.7.9"
  }
}
