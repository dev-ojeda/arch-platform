{
  "name": "@arch/tooling",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "sideEffects": false,
  "files": [
    "dist"
  ],
  "arch": {
    "build": {
      "builder": "tsc",
      "outputs": [
        "dist"
      ],
      "validateExports": true
    }
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "build:dts": "tsc -p tsconfig.build.json --emitDeclarationOnly",
    "arch": "arch build --package @arch/tooling",
    "clean": "arch clean",
    "lint": "arch lint",
    "typecheck": "arch typecheck"
  },
  "dependencies": {
    "@arch/build-core": "workspace:*",
    "@arch/infrastructure": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "execa": "catalog:"
  }
}
