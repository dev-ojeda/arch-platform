{
  "name": "@arch/tooling",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "sideEffects": false,
  "files": [
    "dist"
  ],
  "arch": {
    "artifactType": "runtime",
    "build": {
      "builder": "tsc",
      "outputs": [
        "dist"
      ],
      "validateExports": true
    },
    "kind": "tooling"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "build:dts": "tsc -p tsconfig.build.json --emitDeclarationOnly",
    "arch": "arch build --package @arch/tooling",
    "clean": "arch clean",
    "compliance": "arch compliance --package @arch/tooling --env dev",
    "typecheck": "arch typecheck"
  },
  "dependencies": {
    "@arch/build-core": "workspace:*",
    "@arch/infrastructure": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "execa": "catalog:"
  }
}
