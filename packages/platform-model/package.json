{
  "name": "@arch/platform-model",
  "version": "0.1.0",
  "type": "module",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts"
    }
  },
  "arch": {
    "build": {
      "builder": "tsc-declaration",
      "outputs": [
        "dist"
      ]
    },
    "kind": "tooling"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "arch": "arch build --package @arch/platform-model",
    "clean": "arch clean",
    "typecheck": "arch typecheck"
  }
}
